# Contradiction, Ambiguity, and Discrepancy Register

This register records all discrepancies, alias mismatches, notation conflicts, and potential semantic ambiguities discovered across the research corpus.

## Discrepancy Table

| ID | Domain / Location | Description of Conflict or Ambiguity | Canonical Resolution in Harness | Impact Level |
| :--- | :--- | :--- | :--- | :--- |
| `DISC-001` | Filename Alias | `README.md` (lines 7, 66), `DECISIONS.md` (line 9), and `RIEMANN_MICROSCOPE_CODING_AGENT_PROMPT.md` (line 3) refer to `SPEC.md`, but the file in the repository is named `RIEMANN_MICROSCOPE_SPEC.md`. | Treat `RIEMANN_MICROSCOPE_SPEC.md` as the canonical `SPEC.md` referenced by all documents. Do not rename the root file. | Low (Informational) |
| `DISC-002` | Future Application Layout | `README.md` (lines 46-71) and `SPEC.md` (lines 507-530) describe application modules (`app.py`, `math_core.py`, `transforms.py`, etc.) not yet present at root. | Acknowledge that the root directory currently contains only research specifications. The harness must verify specifications and test vectors without requiring premature application buildout. | Low (Architectural) |
| `DISC-003` | Origin Dilation vs Argument Transform | Potential confusion between coordinate dilation $s'=\tau^K s \implies f_K(s')=\zeta(s'/\tau^K)$ (which maps zeros to $\rho'=\tau^K\rho$) and argument transform $f_K(s)=\zeta(\tau^K s)$ (which maps zeros to $s=\rho/\tau^K$). | Enforce distinct object classes in `MATH_CONTRACT.md` and verification suite. Origin dilation dilates zeros; argument transform contracts zeros. | Critical (Mathematical) |
| `DISC-004` | $\tau$ Symmetry vs Scale Base | Potential misconception that choosing $\tau=2\pi$ implies $\zeta(\tau^K s) = \zeta(s)$ or that $\tau$ is a non-trivial automorphism of $\zeta$. | `DECISIONS.md` §2026-08-19 explicitly mandates $\tau$ is a coordinate convention, NOT a zeta automorphism. Flag any claim of $\tau$-invariance of $\zeta$ as `Contradicted` / `Falsified`. | Critical (Mathematical) |
| `DISC-005` | Centrifuge Modulus Interpretation | Potential circular claim that $|q_\rho^K|=1$ "proves" $\delta=0$. | $|q_\rho^K| = \tau^{K\delta}$, so $|q_\rho^K|=1 \iff \delta=0$ is an exact algebraic equivalence, not a proof that nontrivial zeros of $\zeta(s)$ satisfy $\delta=0$. Flag circularity if asserted as an RH proof. | Critical (Mathematical) |
| `DISC-006` | Dirichlet Series in Critical Strip | Evaluating $\sum_{n=1}^N n^{-s}$ inside the critical strip ($\Re(s)<1$) as a representation of $\zeta(s)$. | Forbidden by `MATH_CONTRACT.md` §14 and `DECISIONS.md`. Analytic continuation via Arb/Flint or mpmath must be used for all critical-strip evaluations. | High (Numerical) |
| `DISC-007` | Reference Zero Seeding in Discovery | Using external reference zeros as initial root brackets or Newton seeds during baseline zero finding. | Strictly forbidden by `DATA_PROVENANCE.md` §1. Zero finder must scan $Z(t)$ independently before post-hoc comparison with reference table. | High (Methodological) |
| `DISC-008` | Complex Branch Cut in $\operatorname{Li}(x^\rho)$ | Riemann Converter involves $x^\rho = \exp(\rho \log x)$ and $\operatorname{Li}(x^\rho)$. Different branch cuts for $\log x$ or $\operatorname{Li}$ could yield discontinuous prime staircase reconstructions. | Document principal branch $\log x \in \mathbb{R}$ for $x>1$ and standard analytic continuation of logarithmic integral $\operatorname{Ei}(\rho \log x)$. Unit tests must lock this convention. | High (Numerical) |
