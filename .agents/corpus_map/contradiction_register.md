# Contradiction, Ambiguity, and Discrepancy Register

This register records all discrepancies, alias mismatches, notation conflicts, and potential semantic ambiguities discovered across the research corpus.

## Discrepancy Table

| ID | Domain / Location | Description of Conflict or Ambiguity | Canonical Resolution in Harness | Impact Level |
| :--- | :--- | :--- | :--- | :--- |
| `DISC-001` | Filename Alias | `README.md` (lines 7, 66), `DECISIONS.md` (line 9), and `RIEMANN_MICROSCOPE_CODING_AGENT_PROMPT.md` (line 3) refer to `SPEC.md`, but the file in the repository is named `RIEMANN_MICROSCOPE_SPEC.md`. | Treat `RIEMANN_MICROSCOPE_SPEC.md` as the canonical `SPEC.md` referenced by all documents. Do not rename the root file. | Low (Informational) |
| `DISC-002` | Future Application Layout | `README.md` (lines 46-71) and `SPEC.md` (lines 507-530) describe application modules (`app.py`, `math_core.py`, `transforms.py`, etc.) not yet present at root. | Acknowledge that the root directory currently contains only research specifications. The harness must verify specifications and test vectors without requiring premature application buildout. | Low (Architectural) |
| `DISC-003` | Origin Dilation vs Argument Transform | Potential confusion between coordinate dilation $s'=\tau^K s \implies f_K(s')=\zeta(s'/\tau^K)$ (which maps zeros to $\rho'=\tau^K\rho$) and argument transform $f_K(s)=\zeta(\tau^K s)$ (which maps zeros to $s=\rho/\tau^K$). | Enforce distinct object classes in `MATH_CONTRACT.md` and verification suite. Origin dilation dilates zeros; argument transform contracts zeros. | Critical (Mathematical) |
| `DISC-004` | $\tau$ Symmetry vs Scale Base | Potential misconception that choosing $\tau=2\pi$ implies $\zeta(\tau^K s) = \zeta(s)$ or that $\tau$ is a non-trivial automorphism of $\zeta$. | `DECISIONS.md` §2026-08-19 explicitly mandates $\tau$ is a coordinate convention, NOT a zeta automorphism. Flag any claim of $\tau$-invariance of $\zeta$ as `Contradicted` / `Falsified`. | Critical (Mathematical) |
| `DISC-005` | Centrifuge Modulus Interpretation | Potential circular claim that $|q_\rho^K|=1$ "proves" $\delta=0$. | $|q_\rho^K| = \tau^{K\delta}$, so $|q_\rho^K|=1 \iff \delta=0$ is an exact algebraic equivalence, not a proof that nontrivial zeros of $\zeta(s)$ satisfy $\delta=0$. Flag circularity if asserted as an RH proof. | Critical (Mathematical) |
| `DISC-006` | Dirichlet Series in Critical Strip | Evaluating $\sum_{n=1}^N n^{-s}$ inside the critical strip ($\Re(s)<1$) as a representation of $\zeta(s)$. | Forbidden by `MATH_CONTRACT.md` §14 and `DECISIONS.md`. Analytic continuation via Arb/Flint or mpmath must be used for all critical-strip evaluations. | High (Numerical) |
| `DISC-007` | Reference Zero Seeding in Discovery | Using external reference zeros as initial root brackets or Newton seeds during baseline zero finding. | Strictly forbidden by `DATA_PROVENANCE.md` §1. Zero finder must scan $Z(t)$ independently before post-hoc comparison with reference table. | High (Methodological) |
| `DISC-008` | Complex Branch Cut in $\operatorname{Li}(x^\rho)$ | Riemann Converter involves $x^\rho = \exp(\rho \log x)$ and $\operatorname{Li}(x^\rho)$. Different branch cuts for $\log x$ or $\operatorname{Li}$ could yield discontinuous prime staircase reconstructions. | Document principal branch $\log x \in \mathbb{R}$ for $x>1$ and standard analytic continuation of logarithmic integral $\operatorname{Ei}(\rho \log x)$. Unit tests must lock this convention. | High (Numerical) |
| `DISC-009` | Gauge Invariance vs Arithmetic Spectrum Restriction | Potential conflation between coordinate covariance gauge $\mathcal{G}_K$ ($\mathcal{Z}_\tau(\tau^K\rho, K) = \zeta(\rho)=0$) and positive-grade arithmetic constraints $\mathcal{A}_K$. | Explicitly separate $\mathcal{G}_K$ (tautological coordinate covariance holding for all continuous dilations) from $\mathcal{A}_K$ (arithmetic constraints). Never assume $\mathcal{G}_K \implies \mathcal{A}_K$ or that $(\bigcap_{K \ge 1} \mathcal{A}_K) \cap \mathfrak{S}_{\mathrm{mixed}} = \varnothing$ is resolved. | Critical (Mathematical) |
| `DISC-010` | Explicit Formula Grade Covariance vs Undefined Joint Constraints | Potential confusion between explicit formula coordinate pullback $\mathcal C_{K,j}[H] \equiv \mathcal C_0[H \circ a_K]$ (`coordinate_redundant`) and prospective cross-grade joint arithmetic constraints. | Enforce that $\mathcal C_{K,j}$ is strictly coordinate-redundant with native explicit formula evaluations. Any candidate for a joint arithmetic constraint operating without test-function dilation must be labeled `OPEN / UNDEFINED CANDIDATE` and never conflated with $\mathcal C_{K,j}$. | Critical (Mathematical) |
| `DISC-011` | Single-Target Energy Positivity vs Subspace Cone Compensation | Potential conflation between single-target quadratic energy $E(u) > 0$ and universal non-compensation across all zero columns. | Enforce distinction: single-target energy $E(u_n) > 0$ does NOT preclude non-negative linear combinations of remaining columns from approximating target columns in a high-nullity finite subspace (as observed in 8/16 cases in `explicit-formula-radial-second-variation-001`). | Critical (Mathematical) |
| `DISC-012` | The Projection Trap | Assuming critical-line projected divisor $\mathcal P_0(\mathcal D_\zeta)$ has an independent arithmetic explicit formula representation. | Enforce distinction: $\mathcal D_\zeta$ has an arithmetic representation, but $\mathcal P_0(\mathcal D_\zeta)$ does not; inferring radial rigidity from $\Delta\mathcal D_{\mathrm{rad}}$ remains the open missing theorem (OBL-EF-003). | Critical (Mathematical) |
| `DISC-013` | Withdrawn EF-013 Gamma-Curvature Claim | Historical working conjecture that EF-013 failed because the explicit formula had the "wrong $\gamma$-curvature". | Audited exact derivation proves $H(z)=\log z$ projection-subtraction yields $2\Re\log(\delta+i\gamma) - 2\Re\log(i\gamma) = \log(1+\delta^2/\gamma^2) = d(\delta,\gamma)$ matching $L_Q$ defect exactly. Former claim is WITHDRAWN; failure is correctly attributed to test function inadmissibility, projected-divisor arithmetic lack, and unproved finite compensation. | Critical (Mathematical) |
| `DISC-014` | Spectral Equivalence of $L_Q=1$ vs Arithmetic RH Proof | Conflating the exact spectral equivalence $L_Q = 1 \iff \mathrm{RH}$ (or $\operatorname{Tr}\mathcal R = 0 \iff \mathrm{RH}$) with an arithmetic proof of RH. | Enforce that $L_Q = 1$ is an exact spectral criterion on the zero divisor, NOT an arithmetic proof of RH. An arithmetic proof requires a divisor-independent arithmetic realization of $\operatorname{Tr}\mathcal R$ or $\det_{\mathrm F}(I+\mathcal R)$. | Critical (Mathematical) |
| `DISC-015` | Bilinear Cross-Terms vs Diagonal Pair Isolation | Expecting bilinear prime-power products $D_K(s)\overline{D_L(s)}$ to isolate reflection pairs $(\lambda, \lambda^\#)$. | Bilinear explicit formula produces an unrestricted double sum $\sum_{\rho_1,\rho_2} \dots$; involution-pair isolation is not obtained, and limiting off-diagonal cancellation remains unproved. | Critical (Mathematical) |
| `DISC-016` | Grade-Limit Raw Coordinate Compression vs Normalized Radial Invariant | Claiming that grade boundaries $\tau^{\pm K} \to 0$ force $\delta = 0$ via raw coordinate compression. | Normalized radial ratio $(\tau^K\delta)^2/(\tau^K\gamma)^2 = \delta^2/\gamma^2$ is strictly grade-invariant; raw coordinate compression does not change the radial class $\delta$. | Critical (Mathematical) |
| `DISC-017` | Cramér Mean-Square Obstruction Scope | Conflating raw 1-parameter power signals $x^{\rho-1/2} = e^{\delta\log x}e^{i\gamma\log x}$ (which require circular $\Theta$-dependent normalization) with all independently parameterized sesquilinear forms. | Enforce distinction: raw signals coupling amplitude and frequency into a single variable suffer the Cramér obstruction; independently parameterized forms must be evaluated for their own specific analytic kernel and convergence class. | Critical (Mathematical) |
| `DISC-018` | Translation Averaging vs Montgomery Pair Correlation | Claiming that every frequency-projected translation limit $\lim_{T\to\infty} \frac{1}{2T}\int_{-T}^T |S(t)|^2 dt$ requires Montgomery pair correlation. | Enforce distinction: when spectral coefficients satisfy $\sum |a_K(\lambda)| < \infty$ ($\ell^1$ absolute summability), dominated convergence justifies the limit interchange directly; pair correlation is required only in weak-decay regimes lacking domination. | Critical (Mathematical) |
| `DISC-019` | Non-Holomorphic Arithmetic Firewall | Attempting to apply the Riemann-Weil explicit formula to non-holomorphic pairings such as $\rho - \rho^\# = 2\delta$. | Direct one-point holomorphic realization vanishing on the critical line vanishes identically everywhere on $\mathbb C^2$ by the Identity Theorem; non-holomorphic pairing cannot be directly evaluated via single-point Cauchy residue calculus; nonlinear sesquilinear constructions remain open. | Critical (Mathematical) |
| `DISC-020` | Transcendental Cross-Grade Resonance Scope | Claiming $\tau^{-K}\log n \ne \tau^{-L}\log m$ is "proved" purely by the transcendence of $\tau = 2\pi$. | Transcendence of $\tau$ does not immediately rule out algebraic dependencies between $2\pi$ and ratios of logarithms of distinct primes/integers because ratios $\log m / \log n$ can be transcendental. Classify finite bounded box frequency gap searches strictly as `NUMERICAL_SEARCH_EVIDENCE_ONLY_NOT_PROOF`. Exact non-resonance for $2\pi$ remains an open problem in transcendental number theory. Single-grade coordinate redundancy is PROVED. | High (Epistemic) |
| `DISC-021` | Besicovitch Translation Mean-Square vs $L^2$ Zero Resolvents | Expecting raw translation averaging $(1/2T)\int_{-T}^T dt$ of Dirichlet polynomials and zero resolvents to extract discrete positive zero-zero quadratic forms. | Dirichlet series possess almost-periodic frequencies $\log n$, yielding non-zero diagonal sums $\sum \Lambda(n)^2 n^{-2\sigma}$ under Besicovitch averaging. In contrast, discrete zero resolvent poles $\frac{1}{\sigma-\rho+it}$ belong to $L^2(\mathbb R, dt)$ with finite $L^2$ norm $\frac{\pi}{\sigma-\Re\rho}$; under $(1/2T)$ scaling, their individual and cross-term integrals vanish as $O(1/T) \to 0$. Extracting discrete positive radial curvature requires regularized Mellin-type kernels rather than raw unweighted translation averages. | Critical (Mathematical) |
| `DISC-022` | Finite Spectral Expansion Closure vs Infinite Spectral Interchange (Gate G4) | Assuming that numerical closure of the finite spectral expansion $S_{N,T}(\sigma) = I_{AA} - I_{AZ} - I_{ZA} + I_{ZZ}$ proves an infinite spectral bridge to the arithmetic anchor. | Enforce strict distinction: exact finite expansion closure is an algebraic 4-term decomposition verified numerically (< 10^{-15}) via closed kernels $J_T, K_T$. However, every fixed finite collection of individual resolvents contributes 0 after $1/(2T)$ normalization as $T\to\infty$. Termwise infinite limit interchange is not justified. Gate G4 (Infinite Spectral Interchange) is the exact earliest open barrier in the present CMSA derivation. | Critical (Mathematical) |
| `DISC-023` | Fixed-Truncation Limit Vanishing vs Cofinal Schedule Boundary Layer Divergence | Assuming that $\forall H < \infty, \lim_{T\to\infty} \sum_{|\gamma|\le H} J_T = 0$ implies that cofinal sums $\lim_{T\to\infty} \sum_{|\gamma|\le H(T)} J_T$ vanish. | Enforce distinction: for any fixed $H$, the sum vanishes as $N(H)\frac{\pi}{2aT} \to 0$, whereas for a cofinal schedule $H(T) = cT$, the diagonal sum diverges logarithmically as $\frac{c\log T}{4a} \to +\infty$. Proved in Lean 4 (`cofinal_schedule_distinct_from_fixed_limit`): $\forall H, \lim_{T\to\infty} f(H,T) = 0 \centernot\implies \lim_{T\to\infty} f(H(T),T) = 0$. | Critical (Mathematical) |
| `DISC-024` | Unrestricted Finite Radial Positivity vs Parameter-Dependent Sign Inversion | Assuming that the unmodified full finite-window difference $\Delta S_W = \int W_T(t) (|A-Z_\delta|^2 - |A-Z_0|^2) dt$ is unconditionally positive whenever $T > \gamma$. | Enforce distinction: the leading second-order variation is $\Delta S_W = \delta^2 C_W + O(\delta^4)$ with $C_W = -2\Re \int W_T(t) F_0(t) \overline{D_\gamma(z)} dt$. On domains with large $\sigma$, small $T$, or strong downweighting of resonance frequencies, $C_W < 0$ and $\Delta S_W < 0$. Fejér witness WIT-02 is certified strictly negative across full symmetric support $[-16.8, 16.8]$ via outward-rounded Arb ball integration (`certify_g4_fejer_witness_arb`). The raw finite Fejér response and additive scalar class are classified strictly as `FAIL_RADIAL_POSITIVITY`, while full infinite/cofinal CMSA-1 & CMSA-2 remain `INCONCLUSIVE_WITH_PRECISE_EARLIEST_OPEN_SUBGATE`. | Critical (Mathematical) |
| `DISC-025` | Additive Reference Subtraction vs Radial Sign Renormalization | Assuming that an additive zero-free scalar reference subtraction $S_W(Z) - R_W(A)$ can repair or renormalize a negative radial sign. | Enforce distinction: for any scalar $R$ independent of zero configurations, $(S_W(Z_\delta) - R) - (S_W(Z_0) - R) \equiv S_W(Z_\delta) - S_W(Z_0)$ identically. Additive reference subtractions are invariant and cannot alter the sign. Any repair requires modifying the integrand, inner product, or operator directly. Proved in Lean 4 (`additive_reference_subtraction_invariance`). | Critical (Mathematical) |
| `DISC-026` | Discrete Grade Covariance vs Unique Schedule Selection | Assuming that bilateral discrete grade covariance $H(\tau T) = \tau H(T)$ ($\tau = 2\pi$) uniquely determines the cofinal zero truncation schedule $H(T)$. | Enforce distinction: every linear schedule $H_c(T) = cT$ ($c > 0$) is discrete grade-covariant, as is every periodic modulation $H(T) = T q(\log_\tau T)$ with 1-periodic $q$. Grade covariance alone does NOT uniquely select a schedule (Lean 4 `grade_covariant_schedule_nonuniqueness`). Collapsing periodic freedom requires asymptotic existence $\lim H(T)/T = c$, and fixing $c \ge 1$ remains explicitly unproved (sharp Hadamard remainder control does not formally force $c \ge 1$ by proved estimate alone). | Critical (Mathematical) |
| `DISC-027` | Exact Remainder Cancellation vs Schedule-Coupled Functional | Assuming that introducing an exact Hadamard remainder $R_H = \Xi'/\Xi - Z_H$ into $S_T(Z_H; R_H) = \frac{1}{2\pi}\int W_T |Z_H + R_H|^2$ creates a schedule-dependent cofinal bridge. | Enforce distinction: $Z_H + R_H \equiv \Xi'/\Xi$ algebraically, so $S_T(Z_H; R_H)$ is identically independent of $H$ and $H(T)$, collapsing to full completed function evaluation. Formally verified in Lean 4 (`exact_remainder_cancellation`, `functional_decomposition_independence`). Candidate classified as `FAIL_LIMIT_ORDER_DEPENDENCE` (descriptive reason `COLLAPSED_COFINAL_IDENTITY`). | Critical (Mathematical) |
| `DISC-028` | Centered Dilation Formula vs Origin Dilation in TC | Describing $s \to 1/2 + \tau^K(s-1/2)$ as the repository's origin grade transformation. | Enforce distinction: In TC, origin coordinate dilation is $s_K = \tau^K s$, with real origin shift $c_K = \tau^K/2$ and centered coordinate dilation $z_K = s_K - c_K = \tau^K(s-1/2)$. On the imaginary axis, ordinate scales as $t' = \tau t$, deriving $H(\tau T) = \tau H(T)$. | High (Notation / Geometry) |
| `DISC-029` | Outer Additive Scalar Invariance vs Inner Background Dependence in Squared Norms | Assuming that `additive_reference_subtraction_invariance` implies that the variation $Q(F_0, \Delta) = |F_0 + \Delta|^2 - |F_0|^2$ reduces to the isolated zero background $-Z_0$. | Enforce distinction: $Q(F, \Delta) = |\Delta|^2 + 2\Re(F\bar\Delta)$ and $Q(F, \Delta) - Q(G, \Delta) = 2\Re((F-G)\bar\Delta)$. Backgrounds placed inside squared norms expand nonlinearly. Shifting background from $-Z_0$ to $F_0 - Z_0$ introduces a nonzero cross-term $2\Re(F_0\bar\Delta)$; Case B does not automatically reduce to the certified finite Fejér witness. Proved in Lean 4 (`complex_squared_norm_difference_expansion`, `complex_squared_norm_difference_background_subtraction`, `complex_squared_norm_difference_not_background_independent`). | Critical (Mathematical) |
| `DISC-030` | Fixed Finite Zero Perturbation vs Nonzero Infinite Mean Response | Assuming that perturbing a fixed finite number of zeros produces a nonzero signal under normalized infinite averaging $\lim_{T\to\infty} \frac{1}{2T}\int_{-T}^T dt$. | Enforce distinction: Fixed Finite Perturbation Invisibility Theorem proves that for any $\sigma > 1$ and any fixed finite resolvent sum $\Delta \in L^2(\mathbb R)$, $\lim_{T\to\infty} \frac{1}{2T}\int_{-T}^T (|P_\sigma - \Delta|^2 - |P_\sigma|^2) dt = 0$. Fixed finite perturbations produce zero response in the limit. Specifically distinguished across epistemic tiers: exact analytical paper proof documented in `CMSA_GATE_G4.md` §9.5; energy scaling $E/(2T) \to 0$ formalized in Lean 4 (`fixed_finite_energy_scaling_zero`, **FORMALLY_PROVED COMPONENT**); finite Dirichlet-polynomial quadrature verified numerically in Python `tests/test_fixed_finite_perturbation_invisibility.py` (**NUMERICAL_EVIDENCE**). | Critical (Mathematical) |
| `DISC-032` | Continuous Grade Differentiation vs Integer Checkpoint Lattices | Conflating the continuous grade parameter $k \in \mathbb R$ used for derivatives, character variation, and curvature $B_\rho''(0)$ with discrete integer checkpoints $K \in \mathbb Z$ generating lattices $L_K = \tau^K\mathbb Z$. | Enforce strict notation: continuous $k \in \mathbb R$ for differential variation and curvature ($B_\rho''(0) = \left.\frac{d^2}{dk^2}B_\rho(k)\right|_{k=0}$); discrete $K \in \mathbb Z$ for bilateral lattices $L_K = \tau^K\mathbb Z$ and integer-indexed checkpoints. | Medium (Notation) |
| `DISC-033` | Detector-Level Finite Zero Visibility vs Arithmetic Descent | Conflating detector-level visibility $B_\rho''(0) = 2\delta^2(\log\tau)^2 > 0$ with an arithmetic proof of RH, or assuming it solves CMSA Gate G4. | Enforce distinction: Curvature transport evaluates the native second grade variation, bypassing fixed-finite $L^2$ translation invisibility at the spectral detector level. However, it does NOT solve CMSA Gate G4. Proving RH requires constructing a zero-independent non-scalar arithmetic functional $\mathscr A_\tau(\xi)$ matching $\sum W_\rho \delta_\rho^2$ (`OBL-CT-001A`), which remains open. | Critical (Mathematical) |
| `DISC-034` | Pure Local Prime Distribution Indefiniteness vs GNS Positivity | Assuming that local prime data alone can factorize into a positive-definite Hilbert space inner product without global Archimedean and pole cancellation. | Enforce distinction: pure local prime distribution weights \(-\frac{\Lambda(n)}{\sqrt{n}}\) are strictly negative-definite (all eigenvalues strictly negative). Positivity \(Q_W(f * f^*) \ge 0\) is a global property strictly equivalent to RH (Weil 1952); assuming positivity a priori without zero-independent positive factorization is circular (`OPEN_GLOBAL_POSITIVE_TYPE_FACTORIZATION`). | Critical (Mathematical) |
| `DISC-035` | Fixed-Zero Multiplier vs Moving Zero Worldline | Conflating fixed-zero scalar multiplication \(F(k,s)=g(k,s)L(s)\) with coordinate-pulled zero worldlines \(L_k(s_\rho(k))\). | Enforce distinction: for fixed point \(\rho\) (\(L(\rho)=0\)), \(F(k,\rho)=0\) and \(\partial_k^m F(k,\rho)=0\) for all \(m \ge 0\). For coordinate-pulled family \(L_k(s)=L(1/2+\tau^{-k}(s-1/2))\) along moving worldline \(s_\rho(k)=1/2+\tau^k(\rho-1/2)\), \(L_k(s_\rho(k))=L(\rho)=0\). Evaluating unpulled static \(L(s_\rho(k))=(\tau^k-1)(\rho-1/2) \ne 0\) generically. (Lean 4 `coordinate_pulled_affine_zero_worldline`, `unpulled_affine_zero_worldline_eval`). | High (Mathematical / Logic) |
| `DISC-036` | Naive Indicator Mellin Transform vs Spectral Probe $1/s$ | Claiming that $g_0(x) = x^{-1/2}\mathbf 1_{[1, \tau]}(x)$ has Mellin transform $1/s$ and directly evaluates $Q_W(g_0) = C_\xi$ and $Q_H(g_0) = N_\xi$. | Enforce distinction: $\widehat g_0(s) = \frac{\tau^{s-1/2}-1}{s-1/2} \ne 1/s$. The probe $1/s$ corresponds to $\mathbf 1_{(0,1)}$ ($f_0(u) = e^{u/2}\mathbf 1_{(-\infty, 0)}(u)$), which is not in $C_c^\infty$. It requires an admissible smoothing family $f_\varepsilon \in C_c^\infty(\mathbb R)$ with certified limit interchange (`FAIL_TEST_FUNCTION_IDENTIFICATION` / `OPEN_ADMISSIBLE_PROBE_REGULARIZATION`). | Critical (Mathematical) |
| `DISC-037` | Mixed Multiplicative vs Additive Coordinate Systems | Simultaneously using multiplicative $x \in \mathbb R_{>0}$ variables and additive logarithmic evaluations at $\pm\log n$ without explicit change of variables. | Enforce distinction: unify under additive logarithmic coordinates $u = \log x \in \mathbb R$, centered Fourier–Laplace transform $\Phi_f(s) = \int_{\mathbb R} f(u) e^{(s-1/2)u} du$, involution $f^*(u) = \overline{f(-u)}$, and $\Phi_{f * f^*}(s) = \Phi_f(s)\overline{\Phi_f(1-\bar s)}$. | High (Notation / Analysis) |
| `DISC-038` | Two-Bump Prime Gram Matrix Indefiniteness vs Diagonal Negative Definiteness | Overclaiming that the prime-only Weil form is strictly negative-definite based on a naive diagonal list. | Enforce distinction: evaluating a genuine two-bump witness separated by $\log p$ yields the $2\times 2$ Gram matrix $\begin{pmatrix} 0 & -w_p \\ -w_p & 0 \end{pmatrix}$ with eigenvalues $+w_p$ and $-w_p$. The prime-only form is indefinite (not strictly negative-definite, not positive semidefinite), maintaining `FAIL_NAIVE_PRIME_LOCAL_FACTORIZATION`. | Critical (Mathematical) |
| `DISC-039` | Bilateral Grade Centering Second Variation vs Asymmetric Coordinate Dilation | Assuming that symmetric second differencing $\mathcal C_h = \mathcal M_h + \mathcal M_{-h} - 2\mathcal M_0$ unconditionally eliminates the background cross-term under coordinate dilation. | Enforce distinction: while exact opposite perturbations $\Delta_{-h} = -\Delta_h$ eliminate the background cross-term ($2|\Delta_h|^2 \ge 0$), coordinate dilation $\Delta_{\pm h}(z) = \Delta Z(\tau^{\pm h} z)$ is asymmetric at second order ($\Delta_h + \Delta_{-h} = 2h^2 B(z) \ne 0$), leaving an uncancelled background cross-term $4h^2\Re(F\bar B) \ne 0$. Proved in Lean 4 (`bilateral_second_order_asymmetry_cross_term` as `ALGEBRAIC_IDENTITY`, and `bilateral_asymmetry_cross_term_nonzero_of_re_nonzero` as `NO_GO_COMPONENT`). Classified as `FAIL_BILATERAL_CROSS_TERM_CANCELLATION`. | Critical (Mathematical) |
| `DISC-040` | Integrated-$\sigma$ Boundary Removal vs Unnormalized Arithmetic Anchor Divergence | Expecting unnormalized $dt d\sigma$ integration of the mean-square resolvent difference to produce a finite arithmetic anchor. | Enforce distinction: while the quartet resolvent difference $\Delta Z \in L^2(\mathbb R \times [\sigma_0, \infty))$ has finite $L^2$ norm ($\frac{3\pi\delta^4}{2a^5}$), the base prime background $\int_{\mathbb R} |P_\sigma(t)|^2 dt = \infty$ diverges completely for fixed $\sigma > 1$, destroying the arithmetic anchor. Normalized translation averaging restores the anchor but renders the finite defect invisible. Classified as `FAIL_ZERO_ARITHMETIC_ANCHOR_UNDER_UNNORMALIZED_T_LIMIT`. | Critical (Mathematical) |
| `DISC-041` | Finite Grade Pullback Identity vs Asymptotic Coordinate Redundancy | Conflating the finite-interval pullback second difference identity with an asymptotic independent grade invariant. | Enforce distinction: at finite $T$, $\mathcal C_{h,T} = \mathcal M_{0,\tau^h T} + \mathcal M_{0,\tau^{-h} T} - 2\mathcal M_{0,T}$ is an exact algebraic identity (`FINITE_GRADE_PULLBACK_IDENTITY`, Lean 4 `finite_grade_pullback_second_difference_identity`). However, as $T \to \infty$, $\lim_{T\to\infty} \mathcal C_{h,T} = 0$ (`ASYMPTOTIC_GRADE_COORDINATE_REDUNDANCY`). The previous universal non-vanishing claim for the diagonal cross-term is withdrawn (`REPORTED_DIAGONAL_CROSS_TERM_UNIVERSAL_NONVANISHING_WITHDRAWN`); exact cancelling variances $v_*(a) = a^2 - a\frac{S_1(a)}{S_2(a)} > 0$ exist for all $a > 1/\log 2$ (`DIAGONAL_CROSS_TERM_HAS_EXACT_CANCELLING_VARIANCES`). | Critical (Mathematical) |
| `DISC-042` | Diagonal Formula vs Complete Finite-Window Inner Product | Assuming that knowing window variance $\langle t^2\rangle_W$ suffices to diagonalize a finite-window Dirichlet inner product. | Enforce distinction: for any finite window $W \in C_c^\infty(\mathbb R)$, the inner product $\langle F_0, \ddot F_0\rangle_W = \sum_{m,n} c_m \bar d_n \int W(t) e^{-it\log(m/n)} [\dots] dt$ contains non-zero off-diagonal terms $m \ne n$ due to $\widehat W(\log(m/n)) \ne 0$. The diagonal expression $\mathfrak X_{\zeta,\mathrm{diag}}$ is only the $m=n$ fragment, not the complete windowed object (`FULL_WINDOWED_ZETA_CROSS_TERM_DERIVED`, Lean 4 `finite_double_sum_2x2_decomp`). | High (Mathematical / Analysis) |




